import 'package:flutter/material.dart';

class PrdInputWidget extends StatelessWidget {
  const PrdInputWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
